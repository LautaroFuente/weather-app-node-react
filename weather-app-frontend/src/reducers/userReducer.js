export const initialUser = {
    id: 0,
    username: "",
    email: "",
    token: "",
}

export const userReducer = ( state, action ) => {
    switch (action.type) {
        case "":
            
            break;
    
        default:
            return state;
    }
}